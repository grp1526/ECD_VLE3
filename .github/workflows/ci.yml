name:

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Build Step
      run: echo "Building the project..."

    - name: Test Step
      run: echo "Passed"

    - name: Notis
      run: 
        curl -X POST -H 'Content-type: application/json' --data '{"text":"Build and tests completed successfully!"}' ${{ secrets.SLACK_WEBHOOK}}